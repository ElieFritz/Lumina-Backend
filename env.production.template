# ===========================================
# CONFIGURATION PRODUCTION LUMINA AFRICA
# ===========================================
# Copiez ce fichier vers .env et configurez vos valeurs

# ===========================================
# CONFIGURATION DE BASE
# ===========================================
NODE_ENV=production
PORT=3001

# ===========================================
# BASE DE DONNÉES (OBLIGATOIRE)
# ===========================================
# MongoDB Atlas (recommandé pour la production)
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/lumina_africa?retryWrites=true&w=majority

# OU PostgreSQL local (si vous préférez)
# DATABASE_URL=postgresql://lumina_user:lumina_password@localhost:5432/lumina_africa

# ===========================================
# SÉCURITÉ (OBLIGATOIRE)
# ===========================================
# Générez une clé JWT forte (ex: openssl rand -base64 32)
JWT_SECRET=GENERATE_A_STRONG_SECRET_KEY_HERE
JWT_EXPIRES_IN=7d

# ===========================================
# REDIS (OBLIGATOIRE)
# ===========================================
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=GENERATE_A_STRONG_REDIS_PASSWORD

# ===========================================
# ELASTICSEARCH (OPTIONNEL)
# ===========================================
# Si vous utilisez Elasticsearch pour la recherche
ELASTICSEARCH_URL=http://localhost:9200

# ===========================================
# PAIEMENTS (OPTIONNEL)
# ===========================================
# Stripe
STRIPE_SECRET_KEY=sk_live_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY

# Paystack
PAYSTACK_SECRET_KEY=sk_live_YOUR_PAYSTACK_SECRET_KEY
PAYSTACK_PUBLIC_KEY=pk_live_YOUR_PAYSTACK_PUBLIC_KEY

# Mobile Money
ORANGE_MONEY_API_KEY=YOUR_ORANGE_MONEY_API_KEY
MTN_MOBILE_MONEY_API_KEY=YOUR_MTN_MOBILE_MONEY_API_KEY

# ===========================================
# STOCKAGE FICHIERS (OPTIONNEL)
# ===========================================
# AWS S3
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_KEY
AWS_REGION=us-east-1
AWS_S3_BUCKET=lumina-africa-uploads-prod

# ===========================================
# EMAIL (OPTIONNEL)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=YOUR_EMAIL@gmail.com
SMTP_PASS=YOUR_EMAIL_PASSWORD

# ===========================================
# SMS (OPTIONNEL)
# ===========================================
SMS_API_KEY=YOUR_SMS_API_KEY
SMS_SENDER_ID=Lumina

# ===========================================
# FRONTEND & CORS (OBLIGATOIRE)
# ===========================================
# URL de votre frontend déployé
FRONTEND_URL=https://your-frontend-domain.vercel.app
CORS_ORIGIN=https://your-frontend-domain.vercel.app

# ===========================================
# SÉCURITÉ AVANCÉE (OPTIONNEL)
# ===========================================
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# ===========================================
# NOTES IMPORTANTES
# ===========================================
# 1. Remplacez toutes les valeurs "YOUR_*" par vos vraies valeurs
# 2. Générez des mots de passe forts pour JWT_SECRET et REDIS_PASSWORD
# 3. Configurez au minimum : MONGODB_URI, JWT_SECRET, REDIS_PASSWORD, FRONTEND_URL
# 4. Les services optionnels peuvent être configurés plus tard





